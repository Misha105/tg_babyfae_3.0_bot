# Отчет о техническом аудите приложения BabyFae (Telegram Mini App)

**Дата:** 24 ноября 2025 г.
**Аудитор:** GitHub Copilot (Gemini 3 Pro)
**Статус:** Завершен

---

## 1. Резюме (Executive Summary)

Приложение представляет собой качественно спроектированное решение с разделением на Backend (Node.js/Express) и Frontend (React/Vite). Архитектура "Offline-first" на фронтенде обеспечивает отличный пользовательский опыт. Безопасность реализована на высоком уровне (валидация `initData`, защита от IDOR, Rate Limiting).

Однако выявлены **критические проблемы с масштабируемостью**, связанные с отсутствием пагинации при загрузке данных, и потенциальные проблемы совместимости на старых устройствах.

**Общая оценка:** 8/10. Код чистый и безопасный, но требует оптимизации для работы с большими объемами данных.

---

## 2. Безопасность (Security Audit)

Уровень безопасности: **Высокий**.

### ✅ Сильные стороны:
1.  **Аутентификация Telegram:** Реализована корректная проверка подписи `initData` с защитой от атак по времени (`crypto.timingSafeEqual`) и проверкой срока действия (`auth_date`).
2.  **Авторизация (IDOR):** Middleware `verifyUserAccess` надежно защищает данные пользователей, проверяя соответствие ID в токене и ID в запросе.
3.  **Rate Limiting:** Грамотно настроены лимиты запросов:
    *   Общий: 100/15мин.
    *   Строгий (экспорт/импорт): 1/мин.
    *   Backup: 1/мин.
4.  **Заголовки безопасности:** Используется `helmet` с настроенным Content Security Policy (CSP).
5.  **SQL Injection:** Используются параметризованные запросы во всех проверенных местах (`dbAsync.run(sql, [params])`).

### ⚠️ Потенциальные риски:
1.  **CSRF (Cross-Site Request Forgery):** Хотя проверка `Origin` реализована, для API, вызываемого из `iframe` Telegram, основным фактором защиты является `initData`. Текущая реализация надежна, но стоит убедиться, что `initData` не утекает в логи (в коде логгирования это учтено, но стоит проверить настройки продакшн-логгера).

---

## 3. Производительность и Масштабируемость (Performance & Scalability)

Уровень: **Средний**. Здесь находятся основные проблемы.

### 🔴 Критические проблемы:
1.  **Отсутствие пагинации (Pagination):**
    *   **Файл:** `bot/src/handlers/data.ts`, функция `getUserData`.
    *   **Проблема:** Запрос `SELECT * FROM activities WHERE telegram_id = ?` выгружает **ВСЮ** историю активностей пользователя за все время.
    *   **Последствия:** С ростом базы данных (через 3-6 месяцев использования) этот запрос будет передавать мегабайты JSON-данных, вызывая задержки загрузки приложения и перерасход памяти на клиенте и сервере.
    *   **Решение:** Внедрить пагинацию (load more / infinite scroll) или загрузку по диапазонам дат.

2.  **Хранение данных (Database Design):**
    *   **Файл:** `bot/src/database/init.ts`.
    *   **Проблема:** Поля `data` в таблицах `activities`, `users` хранятся как `TEXT` (JSON).
    *   **Последствия:** Невозможно эффективно индексировать и искать по внутренним полям JSON (например, "найти все кормления грудью"). SQLite поддерживает JSON-функции, но текущая схема их не использует для оптимизации.

### ✅ Оптимизации:
1.  **SQLite WAL Mode:** Включен режим Write-Ahead Logging, что значительно повышает конкурентность записи/чтения.
2.  **Frontend Optimistic UI:** Использование очереди и локального стейта позволяет приложению работать мгновенно, синхронизируясь в фоне.

---

## 4. Качество кода и Архитектура (Code Quality)

Уровень: **Высокий**.

### ✅ Плюсы:
*   **Типизация:** Строгий TypeScript везде.
*   **Модульность:** Четкое разделение на слои (handlers, middleware, database, utils).
*   **Инфраструктура:** Docker-контейнеры настроены корректно. Nginx на фронтенде правильно проксирует `/api` запросы.

### ⚠️ Недочеты (Code Smells):
1.  **`crypto.randomUUID()` на фронтенде:**
    *   **Файл:** `frontend/src/features/dashboard/Dashboard.tsx`.
    *   **Проблема:** Метод может отсутствовать в старых WebView (Android < 90-ish) или в небезопасных контекстах (хотя Telegram WebApp обычно считается Secure Context).
    *   **Решение:** Использовать полифилл `uuid` (v4).

2.  **Обработка ошибок JSON:**
    *   В `bot/src/handlers/data.ts` используется `safeJsonParse`, что хорошо, но если данные в БД повреждены, пользователь получит `null` или дефолтное значение без явного уведомления об ошибке целостности данных.

---

## 5. План устранения замечаний (Action Plan)

### Приоритет 1: Критические (Сделать сейчас)
1.  [ ] **Внедрить пагинацию API:** Изменить `getUserData` для приема параметров `limit` и `offset` (или `timestamp_before`).
2.  [ ] **Заменить `crypto.randomUUID()`:** Установить пакет `uuid` на фронтенде для гарантированной совместимости.

### Приоритет 2: Важные (Сделать в ближайшее время)
1.  [ ] **Оптимизация БД:** Рассмотреть вынос часто используемых полей из JSON-blob в отдельные колонки (например, `activity_type`, `date`) для индексации.
2.  [ ] **Мониторинг:** Добавить Sentry или аналогичный инструмент для отслеживания ошибок на фронтенде и бэкенде в продакшене.

### Приоритет 3: Улучшения (Backlog)
1.  [ ] **Тесты:** В проекте полностью отсутствуют автотесты (`npm test` выдает ошибку). Необходимо написать хотя бы интеграционные тесты для API.
2.  [ ] **CI/CD:** Настроить пайплайн для автоматической сборки и проверки типов.

---
*Отчет сформирован автоматически на основе анализа исходного кода.*
