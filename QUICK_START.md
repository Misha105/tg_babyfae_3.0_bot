# 🚀 Быстрый старт

## Для разработки

```bash
# 1. Установите зависимости
npm run install:all

# 2. Настройте .env файл
cp .env.example .env
# Отредактируйте .env и добавьте TELEGRAM_BOT_TOKEN

# 3. Запустите dev серверы
npm run dev

# 4. Откройте в Telegram Mini App
# Настройте Menu Button в @BotFather с URL: https://your-tunnel-url
```

## Для production (Docker)

```bash
# 1. Настройте .env
cp .env.example .env
nano .env
# Установите:
# - TELEGRAM_BOT_TOKEN
# - WEBAPP_URL=https://your-domain.com
# - NODE_ENV=production

# 2. Запустите контейнеры
sudo docker compose up -d --build

# 3. Настройте Nginx + SSL
sudo certbot --nginx -d your-domain.com

# 4. Проверьте работу
curl http://localhost:8080/health
```

## Что изменилось в v3.0.0

### ✅ Безопасность
- Добавлена аутентификация через Telegram initData
- Все API эндпоинты защищены
- Валидация всех входных данных
- Rate limiting для защиты от злоупотреблений

### ⚡ Производительность
- Оптимизированы запросы к БД
- Добавлены индексы
- Параллельное выполнение запросов

### 🐛 Исправления
- Исправлены race conditions в транзакциях
- Улучшена обработка ошибок
- Добавлено graceful shutdown

## Важно!

⚠️ **Все API запросы теперь требуют аутентификации**

Фронтенд автоматически отправляет Telegram initData в заголовке `X-Telegram-Init-Data`. Если вы используете API напрямую, убедитесь что отправляете этот заголовок.

## Документация

- **DEPLOY.md** - Детальные инструкции по развертыванию
- **UPGRADE_GUIDE.md** - Обновление с предыдущих версий
- **specs/** - Технические спецификации проекта

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `sudo docker compose logs -f`
2. Проверьте health endpoint: `curl http://localhost:8080/health`
3. Откройте issue на GitHub
